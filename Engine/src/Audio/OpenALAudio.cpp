//Create a new abstract class IAudio to specify the virtual methods its subclasses needs to implement
//Create OpenALAudio class which implements the IAudio; then overrides methods
//Add methods to setand get audio system in IGame
//Create AudioComponent implementing Component.Add a method to play sound, a method to stop sound, and set sound

//Does not need to abstract the concepts(Object, Source, Buffer) into classes yet.

#include "pch.h"