#pragma once
#include "ConsoleLogger.h"

/*template <typename T>
class Expect
{
	T data;
public:
	Expect(const T& val)
		: data(val) { }

	void ToBe(const T& val)
	{
		bool result = data == val;

		if (!result)
			ConsoleLogger::Log("Failed to assert", ConsoleLogger::RED, ConsoleLogger::BLACK);
	}
};*/
